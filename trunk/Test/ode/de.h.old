#ifndef DIFFEQ
#define DIFFEQ

#include<vector>
using std::vector;

class DE
{
	DE(void);
protected:
	double deltat;
public:
	DE(double deltat) : deltat(deltat) {}
	virtual ~DE(void) {}
	virtual void operator()
		( const vector<double>& y, vector<double>& dydt ) const = 0;
};

#endif
